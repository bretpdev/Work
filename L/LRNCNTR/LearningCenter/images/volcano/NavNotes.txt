All navigation images are 218x52 pixels.
The title image is 1000x136 pixels. Since the bottom few pixels are lighter-colored, the nav bar is bumped up 4 pixels to give a smooth transition.

The zero-size images are placeholders for images that don't exist yet, except for navbar.jpg, which isn't used in this theme.