%LET RPTLIB = T:\SAS;
LIBNAME  LEGEND  REMOTE  SERVER=LEGEND SLIBREF=WORK;
RSUBMIT LEGEND;
%LET DB = DNFPUTDL;

LIBNAME PKUB DBX DATABASE=&DB OWNER=PKUB;

PROC SQL ;
	CONNECT TO DBX (DATABASE=&DB);

	CREATE TABLE DEMO AS
		SELECT	
			*
		FROM	
			CONNECTION TO DBX 
				(
					SELECT DISTINCT
						BF_SSN
						,LN_SEQ
						,LD_FAT_EFF AS DEFAULT_DATE
					FROM
						PKUB.LNXX_FIN_ATY
					WHERE
						PC_FAT_TYP = 'XX'
						AND	PC_FAT_SUB_TYP = 'XX'
						AND	LC_FAT_REV_REA = ' '
						AND	LD_FAT_EFF > 'X-XX-XXXX'

					FOR READ ONLY WITH UR
				)
	;
	DISCONNECT FROM DBX;
QUIT;

ENDRSUBMIT;

DATA DEMO; 
	SET LEGEND.DEMO; 
RUN;

/*export to Excel spreadsheet*/
PROC EXPORT DATA = WORK.DEMO 
            OUTFILE = "T:\SAS\NH XXXXX.xlsx" 
            DBMS = EXCEL
			REPLACE;
     SHEET="A"; 
RUN;
