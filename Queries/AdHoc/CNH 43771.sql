DECLARE @SSN TABLE (SSN VARCHAR(X));
INSERT INTO @SSN (SSN) VALUES


--SELECT * FROM @SSN

SELECT DISTINCT
	PDXX.*
FROM
	CDW..PDXX_PRS_BKR PDXX
	INNER JOIN @SSN S
		ON S.SSN = PDXX.DF_PRS_ID
