
LIBNAME  DUSTER  REMOTE  SERVER=DUSTER  SLIBREF=WORK;
RSUBMIT;

PROC SQL;
	CONNECT TO DB2 (DATABASE=DLGSUTWH);

	CREATE TABLE DEMO AS
		SELECT 
			*
		FROM 
			CONNECTION TO DB2 
				(
					SELECT
							*
					FROM	
						OLWHRM1.PD55_PRS_PND_DMO PD55
					WHERE	
						PD55.DF_PRS_ID = '523678429'

					FOR READ ONLY WITH UR
				)
	;

	DISCONNECT FROM DB2;

QUIT;

ENDRSUBMIT;

PROC EXPORT DATA = DUSTER.DEMO 
            OUTFILE = "T:\SAS\NH_17057.xls" 
            DBMS = EXCEL
			REPLACE;
     SHEET="A"; 
RUN;
