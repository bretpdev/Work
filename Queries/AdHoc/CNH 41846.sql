

SELECT * FROM OPENQUERY(LEGEND,
'
SELECT DISTINCT
	RTRIM(PDXX.DM_PRS_X) || '' '' || RTRIM(PDXX.DM_PRS_LST) AS NAME,
	BF_SSN AS SSN,
	LD_LIT_BEG AS BEGIN_DATE
FROM
	PKUB.CLXX_LIT_HST CLXX
	INNER JOIN PKUB.PDXX_PRS_NME PDXX
		ON PDXX.DF_PRS_ID = CLXX.BF_SSN

')






