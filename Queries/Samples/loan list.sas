DATA R7L (DROP=LN_SEQ);
	SET R7 END=LAST;
	LENGTH SEQLIST $200. ;
	BY DF_SPE_ACC_ID LN_SEQ;
	RETAIN SEQLIST;

	IF FIRST.DF_SPE_ACC_ID THEN SEQLIST = LEFT(PUT(LN_SEQ,2.));
	ELSE SEQLIST = CATX(',',TRIM(SEQLIST),LEFT(PUT(LN_SEQ,2.)));

	IF LAST.DF_SPE_ACC_ID THEN OUTPUT;
RUN;
