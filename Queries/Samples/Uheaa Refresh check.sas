
LIBNAME  DUSTER  REMOTE  SERVER=DUSTER  SLIBREF=WORK;
RSUBMIT;

PROC SQL;
	CONNECT TO DB2 (DATABASE=DLGSUTWH);

	CREATE TABLE DEMO AS
		SELECT 
			*
		FROM 
			CONNECTION TO DB2 
				(
					SELECT 
						MAX(AY10.LF_LST_DTS_AY10) AS LastUpdatedRow,
						Current Date
					FROM	 
						OLWHRM1.AY10_BR_LON_ATY AY10					

					FOR READ ONLY WITH UR
				)
	;

	DISCONNECT FROM DB2;


QUIT;

ENDRSUBMIT;

DATA FINAL;
SET DUSTER.DEMO;
RUN;

PROC EXPORT DATA = WORK.FINAL 
            OUTFILE = "T:\SAS\Warehouse refresh check.xls" 
            DBMS = EXCEL
			REPLACE;
RUN;

