CREATE TABLE [dbo].[GENR_DAT_InvalidValues] (
    [ValueKey] VARCHAR (50) NOT NULL,
    [Value]    VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_GENR_DAT_InvalidValues] PRIMARY KEY CLUSTERED ([ValueKey] ASC, [Value] ASC)
);

