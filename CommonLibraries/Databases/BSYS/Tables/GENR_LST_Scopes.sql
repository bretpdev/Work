CREATE TABLE [dbo].[GENR_LST_Scopes] (
    [ScopeOption] VARCHAR (50) NOT NULL,
    [ScopeNumber] INT          NOT NULL,
    CONSTRAINT [PK_GENR_LST_Scopes] PRIMARY KEY CLUSTERED ([ScopeOption] ASC)
);

