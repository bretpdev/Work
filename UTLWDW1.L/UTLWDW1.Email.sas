/*THIS SPECIFICIES WHAT DIRECTORY TO READ REPORTS FROM AND WHICH DATABASE TO UPDATE.*/
/*LIVE*/
%INCLUDE "X:\Sessions\Local SAS Schedule\UDW\UTLWDW1 UHEAA Data Warehouse Load.Folder and Database.sas";
%LET REPORT = X:\PADD\FTP;

/*TEST*/
/*%INCLUDE "X:\PADU\SAS\UDW\UTLWDW1 UHEAA Data Warehouse Load.Folder and Database.sas" ;
%LET REPORT = T:\SAS;*/

%FILECHECK(23);

DATA PD32(drop=trunc) ;
	INFILE REPORT23 DSD DLM = ',' FIRSTOBS=1 MISSOVER end = eof;
	INPUT DF_SPE_ACC_ID :$10. DC_ADR_EML :$1. DX_ADR_EML :$254. DD_VER_ADR_EML :$10. DI_VLD_ADR_EML :$1. ;
%TRUNCAT(23,DF_SPE_ACC_ID);
RUN;
%copyerror;

%ENCHILADA(PD32,PD32_EMAIL,DF_SPE_ACC_ID DC_ADR_EML,DX_ADR_EML DD_VER_ADR_EML DI_VLD_ADR_EML,WHERE DI_VLD_ADR_EML = 'Y');

%GOODBYE_NULL(PD32_EMAIL,DX_ADR_EML,'');
%GOODBYE_NULL(PD32_EMAIL,DD_VER_ADR_EML,'');
%GOODBYE_NULL(PD32_EMAIL,DI_VLD_ADR_EML,'');

%FINISH;
