/*THIS SPECIFICIES WHAT DIRECTORY TO READ REPORTS FROM AND WHICH DATABASE TO UPDATE.*/
/*LIVE*/
%INCLUDE "X:\Sessions\Local SAS Schedule\UDW\UTLWDW1 UHEAA Data Warehouse Load.Folder and Database.sas";
%LET REPORT = X:\PADD\FTP;

/*TEST*/
/*%INCLUDE "X:\PADU\SAS\UDW\UTLWDW1 UHEAA Data Warehouse Load.Folder and Database.sas" ;
%LET REPORT = T:\SAS;*/


%FILECHECK(19);

DATA SD10(drop=trunc);
	INFILE REPORT19 DSD DLM = ',' FIRSTOBS=1 MISSOVER end = eof;
	INPUT DF_SPE_ACC_ID :$10. LN_SEQ :6. LD_SCL_SPR :$10. 
		DOE_SCL_ENR_CUR :$40.  ;
%TRUNCAT(19,DF_SPE_ACC_ID);
RUN;
%copyerror;

PROC SORT DATA=SD10; BY DF_SPE_ACC_ID LN_SEQ; RUN;
DATA SD10; SET SD10; BY DF_SPE_ACC_ID LN_SEQ; IF LAST.LN_SEQ; RUN;

%ENCHILADA(SD10,SD10_SCHOOL,DF_SPE_ACC_ID LN_SEQ,LD_SCL_SPR DOE_SCL_ENR_CUR,);

%GOODBYE_NULL(SD10_SCHOOL,LD_SCL_SPR,'');
%GOODBYE_NULL(SD10_SCHOOL,DOE_SCL_ENR_CUR,'');

%FINISH;
