PROC IMPORT OUT= WORK.POP
            DATAFILE= "T:\UHEAA - Bankruptcy accounts.xlsx" 
            DBMS=EXCEL REPLACE;
     RANGE="Sheet1$"; 
     GETNAMES=YES;
     MIXED=NO;
     SCANTEXT=YES;
     USEDATE=YES;
     SCANTIME=YES;
RUN;

LIBNAME  DUSTER  REMOTE  SERVER=DUSTER  SLIBREF=WORK;

DATA DUSTER.SOURCE; *Send data to Duster;
SET POP;
RUN;

RSUBMIT;  

/*%let DB = DLGSWQUT;  *This is test;*/
%let DB = DLGSUTWH;  *This is live;

LIBNAME OLWHRM1 DB2 DATABASE=&DB OWNER=OLWHRM1;
PROC SQL;
	CREATE TABLE RESULT AS
		SELECT DISTINCT
			S.*, 
			LN10.LN_SEQ, 
			LN10.PF_MAJ_BCH, 
			LN10.PF_MNR_BCH
		FROM
			OLWHRM1.LN10_LON LN10
			INNER JOIN SOURCE S
				ON S.ssn = LN10.BF_SSN
;
QUIT;

ENDRSUBMIT;

DATA RESULT;
SET DUSTER.RESULT;
RUN;

PROC EXPORT DATA = WORK.RESULT 
            OUTFILE = "T:\BK BORROWER WITH BATCHES.xlsx" 
            DBMS = EXCEL
			REPLACE;
     SHEET="Sheet1"; 
RUN;
