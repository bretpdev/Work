CREATE TABLE [dbo].[Current_Def_Forb_Options]
(
	[current_def_forb_option_Id] INT NOT NULL PRIMARY KEY IDENTITY, 
    [current_def_forb_option] VARCHAR(300) NOT NULL, 
    [file_value] CHAR NOT NULL
)
