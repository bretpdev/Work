CREATE FUNCTION [dbo].[DECONVERT_DATE]
(
	@date date
)

RETURNS CHAR(7)
AS

BEGIN
	RETURN CASE WHEN RIGHT(YEAR(@DATE), 2) = 99 THEN '0' ELSE '1' END + RIGHT(YEAR(@DATE), 2) + RIGHT('00' + CAST(MONTH(@DATE) AS VARCHAR(2)), 2) + RIGHT('00' + CAST(DAY(@DATE) AS VARCHAR(2)), 2)
END