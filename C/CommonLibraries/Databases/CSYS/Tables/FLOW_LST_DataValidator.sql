CREATE TABLE [dbo].[FLOW_LST_DataValidator] (
    [Name]   VARCHAR (50) NOT NULL,
    [System] VARCHAR (30) NOT NULL,
    CONSTRAINT [PK_FLOW_LST_DataValidator] PRIMARY KEY CLUSTERED ([Name] ASC)
);

