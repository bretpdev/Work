CREATE TABLE [dbo].[LCTR_REF_Responsible] (
    [ID]    BIGINT        NOT NULL,
    [title] VARCHAR (100) NOT NULL,
    CONSTRAINT [PK_LCTR_REF_ProcedureResponsible] PRIMARY KEY CLUSTERED ([ID] ASC, [title] ASC)
);

